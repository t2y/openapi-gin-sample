{{>partial_header}}
package main

import (
	"log"

	sw "github.com/t2y/openapi-gin-sample/{{apiPath}}"
)

func main() {
	log.Printf("Server started")

	router := sw.NewRouter()

	log.Fatal(router.Run(":{{serverPort}}"))
}
